name: Pull Request Pipeline

on:
  pull_request:
    branches:
      - genaiops-workshop

jobs:
  evaluations:
    runs-on: ubuntu-latest
    environment:
      name: dev

    env:
      AZURE_CLIENT_SECRET:   ${{ fromJson(secrets.AZURE_CREDENTIALS).clientSecret }}
      AZURE_TENANT_ID:       ${{ fromJson(secrets.AZURE_CREDENTIALS).tenantId }}
      AZURE_CLIENT_ID:       ${{ fromJson(secrets.AZURE_CREDENTIALS).clientId }}
      APP_CONFIG_ENDPOINT:   ${{ vars.APP_CONFIG_ENDPOINT }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Debug APP_CONFIG_ENDPOINT
        run: |
          echo "APP_CONFIG_ENDPOINT=[$APP_CONFIG_ENDPOINT]" 

      - name: Login to Azure CLI
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Debug: show access token oid
        run: |
          az account get-access-token --resource https://cosmos.azure.com/ -o json > /tmp/token.json
          OID=$(jq -r '.accessToken' /tmp/token.json | cut -d. -f2 | base64 --decode | jq -r '.oid')
          echo "OID in token: $OID"

      - name: Setup Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Run evaluation
        run: |
          chmod +x evaluations/evaluate.sh
          ./evaluations/evaluate.sh

      - name: Upload evaluation results
        uses: actions/upload-artifact@v4
        with:
          name: evaluation-results
          path: evaluations/evaluation-results.json